<section class="row">
    <div class="desc">
      <h2><%= @position.name %></h2>
    </div>


    <h3>Candidates</h3>
    <%= render 'candidates' %>
    <%= link_to 'Add Candidate', new_position_candidate_path(@position, @comment), class:'button radius small' %>
    <%= link_to 'Edit Position', edit_position_path(@position), class: 'button radius small' %>
</section>
