<%= form_for [@position, @candidate] do |f| %>
  <%= f.label :last_name %>
  <%= f.text_field :last_name %>

  <%= f.label :first_name %>
  <%= f.text_field :first_name %>

  <%= f.label :slogan %>
  <%= f.text_area :slogan %>

  <%= f.submit 'Save' %>
<% end %>
